// A list of all of the posts.

import React from "react"
import { PostListWrapper } from "../elements"

export const PostList = ({ children }) => {
  return <PostListWrapper>{children}</PostListWrapper>
}
